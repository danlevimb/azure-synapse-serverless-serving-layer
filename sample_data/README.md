# Sample Data

This folder documents the controlled synthetic retail datasets used by the project.

The MVP uses curated Parquet files stored in Azure Data Lake Storage Gen2.

Sample data files may be generated locally or documented here, but large generated outputs should not be committed unless intentionally selected for public demonstration.